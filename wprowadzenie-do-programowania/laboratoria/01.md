Witajcie,

Mam taki zwyczaj, że zaczynam od nauczania poprzez przykłady. Na początek proste przykłady różnych podstawowych i w miarę schematycznych zadań.

Waszym zadaniem jest się zapoznać. Wprowadzić do ide, ale już z jakimiś swoimi zmianami. Cel - zrozumieć jak to działa. W miarę postępów, przykładów będzie coraz to mniej a coraz to więcej samodzielnych zadań. Przy czym - pechowo często, w czasie kopiowania potrafią się skasować wcięcia. Więc możliwe, że to trzeba będzie samemu dojść

*__KOPIOWANE Z INNEGO SYSTEMU - mogło gdzieniegdzie wciącia pozmieniać__*

 1\. Program "Hello, World!" w Pythonie:

print("Hello, World!")

 

2\. Program do wyświetlania imienia podanego przez użytkownika:

wiek = input("Podaj swoje imię: ")

print("Twoje imię to:", imie)

3\. Prosty kalkulator w Pythonie: (od razu sposób - jak definiować metody, też nieśmiałe wprowadzenie jak działają if i else)

def dodawanie(a, b):

  return a + b

 

def odejmowanie(a, b):

  return a - b

 

def mnozenie(a, b):

  return a \* b

 

def dzielenie(a, b):

  if b != 0:

  return a / b

  else:

  return "Nie można dzielić przez zero"

 

print("Wybierz operację:")

print("1. Dodawanie")

print("2. Odejmowanie")

print("3. Mnożenie")

print("4. Dzielenie")

 

wybor = input("Wybierz numer operacji (1/2/3/4): ")

 

if wybor in ('1', '2', '3', '4'):

  num1 = float(input("Podaj pierwszą liczbę: "))

  num2 = float(input("Podaj drugą liczbę: "))

 

  if wybor == '1':

  print("Wynik:", dodawanie(num1, num2))

  elif wybor == '2':

  print("Wynik:", odejmowanie(num1, num2))

  elif wybor == '3':

  print("Wynik:", mnozenie(num1, num2))

  elif wybor == '4':

  print("Wynik:", dzielenie(num1, num2))

else:

  print("Błędny wybór")

 

4\. Obliczanie pola powierzchni prostokąta w Pythonie:

dlugosc = float(input("Podaj długość prostokąta: "))

szerokosc = float(input("Podaj szerokość prostokąta: "))

pole = dlugosc \* szerokosc

print("Pole powierzchni prostokąta wynosi:", pole)

5\. Znajdowanie największej i najmniejszej liczby w liście:

lista_liczb = \[3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5\] 

najmniejsza = min(lista_liczb)

najwieksza = max(lista_liczb)

print("Najmniejsza liczba w liście:", najmniejsza)

print("Największa liczba w liście:", najwieksza)

6\. Gra zgadywanka liczb w Pythonie (przykładowa implementacja):

import random (tu jest przykład pierwszego importu biblioteki)

liczba_do_zgadniecia = random.randint(1, 100)

proby = 0

while True:

  zgadywana_liczba = int(input("Zgadnij liczbę (od 1 do 100): "))

  proby += 1

  if zgadywana_liczba == liczba_do_zgadniecia:

  print(f"Brawo! Zgadłeś liczbę {liczba_do_zgadniecia} w {proby} próbach.")

  break

  elif zgadywana_liczba < liczba_do_zgadniecia:

  print("Liczba jest większa.")

  else:

  print("Liczba jest mniejsza.")

Inna wersja kodu:

import random

liczba_do_odgadniecia = random.randint(1, 100)

proby = 0

while True:

guess = int(input("Zgadnij liczbę (1-100): "))

proby += 1

if guess < liczba_do_odgadniecia:

print("Za mało!")

elif guess > liczba_do_odgadniecia:

print("Za dużo!")

else:

print(f"Gratulacje! Zgadłeś liczbę {liczba_do_odgadniecia} w {proby} próbach.")

break

Dodajmy do tego BMI:

### 

1. Poproś użytkownika o podanie swojej masy ciała w kilogramach.
2. Poproś użytkownika o podanie swojego wzrostu w metrach.
3. Oblicz BMI na podstawie podanych wartości. Wzór na BMI to: BMI = masa ciała (kg) / (wzrost (m) \* wzrost (m)).
4. Wyświetl wynik BMI użytkownikowi.
5. Na podstawie wyniku BMI wydrukuj komunikat informujący o stanie zdrowia użytkownika, np. "Masz niedowagę", "Twoja waga jest w normie" lub "Masz nadwagę".

**A może pobawmy się jeszcze w grę:**

1. Gra składa się z kilku "epizodów". Każdy epizod to krótka historia lub wydarzenie w labiryncie.
2. Na początku każdego epizodu gracz będzie dostawał opis sytuacji i będzie musiał wybrać jedną z dwóch lub więcej opcji, wybierając odpowiednią liczbę.
3. Wybory gracza będą miały wpływ na przebieg historii i mogą prowadzić do różnych zakończeń.